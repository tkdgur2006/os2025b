package main

/*
func main() {
	//shadowing
	i, err := bufio.NewReader(os.Stdin)
	if err != nil {
		log.Fatal(err)

	i = strings.TrimSpace(i)
	score, err := strconv.ParseFloat(i, 64)
	if err != nil {
		log.Fatal(err)
	}

	var status string
	if score >= 60 {
		status = "passing"
	} else {
		status = "Fail"
	}

	fmt.Println(status)
	}
*/
func main() {

}
